   container process                 worker / durable object
   --------------------       -------------------------------
   DUMMY_PROVIDER_KEY  --------->  swaps in the real key
   (never a live secret)             (AI Gateway binding)

   allowedHosts: the selected harness provider host + git
   nothing else. never the union across harnesses.

   GITHUB_TOKEN       --------->  attached at egress,
   (never in container)             scoped to /owner/repo
