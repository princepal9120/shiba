# Shiba Journal (/blog) — team context\n\n## Target\nEnd-to-end production blog for Shiba (ai-intern / shiba-ai-coworker), modelled on\nzuse.sh/blog: Journal / Guide / Note categories, 5-8 min posts, H2-scoped guides\nwith an "On this page" TOC, honest-limitations register, cross-linking, byline.\n\n## State: scaffold DONE and committed (aca0ad8) — do not rebuild\n- apps/web/src/content.config.ts — journal collection (glob loader, base\n  ./src/content/journal, generateId strips .md). Zod import is astro/zod\n  (z from astro:content is deprecated in Astro 7).\n- apps/web/src/lib/journal.ts — getPublishedPosts/renderPost/readingTime,\n  CATEGORY_LABEL, PATTERN_LABEL.\n- apps/web/src/layouts/JournalArticle.astro — banner + TOC + byline shell.\n- apps/web/src/pages/blog/ — index.astro, [...id].astro, rss.xml.ts,\n  category/[category].astro\n- apps/web/src/styles/blog.css — imported by MarketingPage.astro (tokens live\n  in that layout's scoped style block, so a page-level import would not resolve).\n- Banner asset convention: apps/web/src/assets/blog/<id>.txt, read via\n  import.meta.glob with query ?raw. A MISSING banner renders a visible notice;\n  the build does not fail. Article route is /blog/<id>/ where <id> is the post\n  filename stem.\n\n## Worker task: ADD CONTENT ONLY\nFor each assigned post create exactly two files:\n1. apps/web/src/content/journal/<slug>.md   (id = <slug>, route /blog/<slug>/)\n2. apps/web/src/assets/blog/<slug>.txt       (the ASCII banner)\n\nPost frontmatter (all required except updatedDate):\n\n    title: string\n    description: string       # one sentence, used as meta + index card\n    pubDate: 2026-09-26      # ISO; stagger dates 2026-09-14 .. 2026-09-26\n    category: guide | journal | note\n    pattern: protagonist-arc | choreography |\n             situation-complication-resolution | what-is-what-could-be\n    summary: string          # one line, plain text, no markdown\n\nBody: markdown only. Start sections with ## (these build the TOC). No H1 in body.\n\n## Honesty invariants — NON-NEGOTIABLE (standing repo rules)\n- Shiba is a LOCAL PROTOTYPE. No live end-to-end cloud run is claimed. Never\n  state or imply a live verified run, success rate, user count, benchmark,\n  latency, or dollar saving.\n- Approval gate is sacred; provider keys never enter the container; GITHUB_TOKEN\n  never reaches container / clone URL / logs / UI; webhook secrets header-only;\n  no fake success.\n- Stale-claim denylist enforced by scripts/check-docs.mjs against\n  apps/web/src/content/docs only, but the blog must not resurrect them either:\n  WORKER_ORIGIN, gemini-2.0, only google, provider callback (unnegated).\n- Every factual assertion must be traceable to README.md, spec/GOAL.md, PLAN.md,\n  VERIFICATION.md, or design.md. Read them before writing.\n- Use the .callout HTML block for the prototype-status disclosure where honest.\n\n## Voice (from zuse.sh, researched)\nPlain, concrete, second person. Short paragraphs. Declarative sentences. No\nmarketing superlatives. Name limits explicitly. Avoid revolutionary / seamless /\npowerful / leverage / unlock / game-changing. Sentences under 25 words where\npossible.\n\n## ASCII banner rules\nMonospace-safe box or figlet art, max ~72 columns wide, no ANSI colour escapes,\nno tabs. Deterministic per slug. Must not assert anything false. 6 to 12 lines.\n\n## Verify\ncd apps/web then pnpm check and pnpm build. Both must pass with 0 errors.\n